<div class="pg">
    {{#if prev}}
        <a class="pg__prev button" href="{{page_url prev}}">
            &larr; Previous Page
        </a>
    {{/if}}

    {{#if next}}
        <a class="pg__next button" href="{{page_url next}}">
            Next Page &rarr;
        </a>
    {{/if}}
</div>
